# System Limitations
The transaction data is synthetic. The local language model is a narrative assistant, not a fraud classifier. RAG quality depends on knowledge coverage. Illustrative cost coefficients require real business estimates. The system has no external identity, merchant, device, geolocation, sanctions, banking or network intelligence.
