# Threshold and Cost
For threshold t, REVIEW occurs when p >= t and ALLOW otherwise. The illustrative cost is Cost(t) = C_FN*FN(t) + C_FP*FP(t) + C_REVIEW*Review(t). A maximum review-rate constraint prevents the threshold optimizer from flagging almost everything. The coefficients are configuration examples, not universal business costs.
