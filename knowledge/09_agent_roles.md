# Agent Roles
Transaction Agent examines transaction facts. Customer Agent summarizes customer history. Terminal Agent summarizes terminal history. Evidence Agent combines model probability, SHAP evidence and retrieved knowledge. Report Agent creates a structured narrative. Supervisor runs the agents in dependency order. Agents are explicit Python tools rather than uncontrolled loops.
