1. Create environment:
   python -m venv .venv
   .venv\Scripts\activate

2. Install:
   pip install -r requirements.txt

3. Put the existing transaction dataset at:
   data/transactions.pkl

4. Run everything:
   python main.py

5. Start Streamlit for UI:
   streamlit run app.py
